doctype html
html(lang='en')
  head
    meta(charset='utf-8')
    title= locals.title
  body
    p
      | This portion of code is part of the template file. The following list
      | of titles is being generated by using the env.helpers.getArticles
      | helper function:
    - articles = env.helpers.getArticles(contents);
    for article in articles
      ul
        li= article.title
